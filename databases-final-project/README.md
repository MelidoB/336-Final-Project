# RepairWave Technologies Backend
To get started, install `pdm` and run `pdm install`. To run the backend, run `pdm run serve.py`

To create all necessary tables, call the `/tables/populate` endpoint.
